const Navbar = () => {
  return (
    <nav className="navbar">
      <h2>StayMate Hostel Management</h2>

      <div>
        <button>Logout</button>
      </div>
    </nav>
  );
};

export default Navbar;